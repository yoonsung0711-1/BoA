{
  "devDependencies": {
    "@types/node": "^13.7.4",
    "tsc-watch": "^4.1.0",
    "typescript": "^3.8.2"
  },
  "scripts": {
    "start": "rm -rf dist && sh -c 'nodemon -e ts -w ./ -x \"tsc --outDir ./dist ${0}.ts && node dist/${0}.js\"'"
  }
}
