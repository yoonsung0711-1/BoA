{
  "devDependencies": {
    "@types/node": "^13.7.4",
    "nodemon": "^2.0.2",
    "tsc-watch": "^4.1.0",
    "typescript": "^3.8.2"
  },
  "scripts": {
    "start": "rm -rf dist && sh -c 'npx tsc-watch --onSuccess \"node dist/${0}.js\"'"
  }
}
